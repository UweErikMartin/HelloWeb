name: build
run-name: build ${{github.repository}} by ${{github.actor}}
on: [push, pull_request]

jobs: 
  # The “build” workflow 
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Perform Checkout
      uses: actions/checkout@v4

    - name: Get Golang Version from go.mod file
      run: sed -En 's/^go (.*)$/GO_VERSION=\1/p' go.mod >> $GITHUB_ENV

    - name: Setup Go Enviroment
      uses: actions/setup-go@v4
      with:
        go-version: '${{ env.GO_VERSION }}' # The Go version to download (if necessary) and use.

    - run: go version

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3      

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build Container Image
      run: |
        docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/helloweb:amd64 -f build/Dockerfile .
    
    - name: Push to registry
      run: |
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/helloweb:amd64                
