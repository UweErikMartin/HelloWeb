apiVersion: v1
kind: Service
metadata:
  name: tom
spec:
  selector:
    app.kubernetes.io/name: tom
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
---
apiVersion: v1
kind: Service
metadata:
  name: jerry
spec:
  selector:
    app.kubernetes.io/name: jerry
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000